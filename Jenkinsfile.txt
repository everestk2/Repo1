pipeline {
    agent any
    tools {
        maven 'Maven 3.3.9'
    }
    stages {
       stage ('Build') {
            steps {
                bat 'mvn -Dmaven.test.failure.ignore=true compile' 
            }
        }
    }
}